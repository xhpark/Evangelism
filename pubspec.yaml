name: just_ee_master
description: "전도폭발 JUST EE 암기 마스터 앱"
publish_to: 'none'

version: 1.0.1+2

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter
  flutter_tts: ^4.2.2
  speech_to_text: ^7.0.0
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  flutter_secure_storage: ^11.0.0
  wakelock_plus: ^1.8.0
  http: ^1.2.2
  package_info_plus: ^10.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - data/just_ee_data.json
    - assets/images/
